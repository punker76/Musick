<Controls:MetroWindow
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Musick"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:Custom="http://metro.mahapps.com/winfx/xaml/controls" x:Class="Musick.MusickError"
        mc:Ignorable="d"
        Title="MusickError" Height="100" Width="300" Icon="Musick.ico" ShowTitleBar="False" ResizeMode="NoResize" SizeToContent="Width" ShowCloseButton="False" ShowMaxRestoreButton="False" ShowMinButton="False" ShowIconOnTitleBar="False" WindowStartupLocation="CenterOwner" IsWindowDraggable="False" IsCloseButtonEnabled="False" WindowTransitionsEnabled="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label x:Name="lblError"
			   Grid.Row="0"
               VerticalAlignment="Center"
               HorizontalAlignment="Center"/>
        <Button x:Name="btnOK"
			Content="Okay"
			Style="{StaticResource ResourceKey=SquareButtonStyle}"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
            Click="btnOK_Click"
			Grid.Row="1" Custom:ButtonHelper.PreserveTextCase="True"/>
    </Grid>
</Controls:MetroWindow>
